import { siteContent } from "@/content/siteContent";
import { SectionHeader } from "@/components/SectionHeader";

export function HowItWorks() {
  return (
    // <section id="como-funciona" className="py-16">
    //   <div className="mx-auto max-w-6xl px-6">
    //     <SectionHeader title={siteContent.howItWorks.title} />
    //     <div className="mt-10 grid gap-5 md:grid-cols-3">
    //       {siteContent.howItWorks.steps.map((step, index) => (
    //         <article
    //           key={step.title}
    //           className="rounded-2xl border border-white/70 bg-white p-6 shadow-soft"
    //         >
    //           <span className="text-sm font-semibold text-accent">0{index + 1}</span>
    //           <h3 className="mt-3 font-display text-xl text-ink">{step.title}</h3>
    //           <p className="mt-2 text-sm text-muted">{step.description}</p>
    //         </article>
    //       ))}
    //     </div>
    //   </div>
    // </section>
    <></>);
}
